<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- In res/values/styles.xml -->
    <style name="FilterButton" parent="Widget.AppCompat.Button">
        <item name="android:background">@drawable/bg_filter_unselected</item> <!-- Important -->
        <item name="backgroundTint">@null</item> <!-- Prevents Material theming from tinting -->
        <item name="android:textAllCaps">false</item>
        <item name="android:textColor">?attr/colorOnSurface</item>
        <item name="android:fontFamily">@font/inter_semibold</item>
        <item name="android:textSize">13sp</item>
        <item name="android:paddingStart">16dp</item>
        <item name="android:paddingEnd">16dp</item>
    </style>
</resources>